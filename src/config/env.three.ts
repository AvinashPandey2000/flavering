import { AppConfig } from './config.types';

export const threeConfic: AppConfig = {
  ENV: 'three',
  API_URL: 'https://api.myapp.com',
  THEME_COLOR: '#0000FF', // Blue for Prod
  DEBUG_MODE: false,
};