import { AppConfig } from './config.types';

export const twoConfic: AppConfig = {
  ENV: 'two',
  API_URL: 'https://staging.api.myapp.com',
  THEME_COLOR: '#FFA500', // Orange for Staging
  DEBUG_MODE: true,
};